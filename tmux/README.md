# tmux container build

From root directory run
```bash
./build.sh .        # To build main build image
./build.sh tmux     # To build tmux

# After successful build tmux binary can be found here
./tmux/dist/tmux
```

